---
title: 시간 및 자재 계약의 실제 값 영향
description: 이 항목은 Microsoft Dynamics 365 Project Operations의 시간 및 자재 계약 수명 주기 동안 다양한 이벤트에서 실제 값 테이블에 미치는 영향에 대한 정보를 제공합니다.
author: rumant
ms.date: 02/22/2022
ms.topic: overview
ms.prod: ''
audience: Application User
ms.reviewer: johnmichalak
ms.search.scope: ''
ms.assetid: ''
ms.search.region: ''
ms.search.industry: ''
ms.author: rumant
ms.search.validFrom: 2020-10-01
ms.openlocfilehash: a4ea3f9cf74d8a67447571001b75065b8cde5c00
ms.sourcegitcommit: c0792bd65d92db25e0e8864879a19c4b93efb10c
ms.translationtype: HT
ms.contentlocale: ko-KR
ms.lasthandoff: 04/14/2022
ms.locfileid: "8580832"
---
# <a name="actuals-impact-in-a-time-and-materials-engagement"></a>시간 및 자재 계약의 실제 값 영향

_**적용 대상:** 리소스/비 재고 기반 시나리오를 위한 Project Operations, Lite 배포 - 견적 송장 처리_

다음 표에는 시간 및 자재 계약의 다양한 이벤트에서 생성된 다양한 트랜잭션 유형의 실제 값이 나열되어 있습니다.

| 이벤트 | 비용 실제값 | 미청구 매출액 실제값 | 청구 매출액 실제값 | 예 |
|---|---|---|---|---|
| 시간이 생성됩니다. | 해당 없음 | 해당 없음 | 해당 없음 | <p>시간당 비용이 100달러(미화 100달러)인 Fabrikam US 조직 구성 단위의 Bob Kozack은 "Adatum에 암 설치"라는 프로젝트를 진행하고 있습니다. 이 프로젝트에서 그의 계약 청구 요금은 시간당 USD 200입니다. 다음은 Bob Kozak의 샘플 시간 항목입니다.</p><p>Bob Kozack, 8시간</p> |
| 시간이 제출됩니다. | 해당 없음 | 해당 없음 | 해당 없음 | 시간 입력에 대해 비용 분개장 라인 및 미청구 판매 분개장이 생성됩니다. 기본 가격과 원가율은 분개장에 입력됩니다. |
| 시간 입력은 승인되기 전에 회수됩니다. | 해당 없음 | 해당 없음 | 해당 없음 | |
| 승인된 시간: 제출된 시간은 청구 가능한 시간과 같습니다. | 비용 실제 값이 생성됩니다. | 미청구 매출액 실제 값이 생성됩니다. | 해당 없음 | <p>생성된 새 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800</li><li>**미청구 매출액 실제값**: Bob Kozack, 8시간, USD 1,600</li></ul> |
| 승인된 시간: 청구 가능 시간이 제출된 시간보다 줄어듭니다. | 비용 실제 값이 생성됩니다. | <p>두 개의 미청구 매출액 실제 값이 생성됩니다.</p><ul><li>청구 가능 시간에 대해 청구 가능한 미청구 매출액 실제 값</li><li>잔여 수량에 대해 청구 불가능한 미청구 매출액 실제 값</li></ul> | 해당 없음 | <p>생성된 새 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800</li><li>**미청구 매출액 실제값:** Bob Kozack, 6시간, USD 1,200 ,*청구 가능*</li><li>**미청구 매출액 실제값:** Bob Kozack, 2시간, USD 400 ,*청구 불가능*</li></ul> |
| 승인된 시간: 청구 가능 시간이 제출된 시간보다 늘어납니다. | 비용 실제 값이 생성됩니다. | 미청구 매출액 실제 값이 생성됩니다. | 해당 없음 | <p>생성된 새 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800</li><li>**미청구 매출액 실제값**: Bob Kozack, 10시간, USD 2,000</li></ul> |
| 시간 승인이 취소됩니다. | <p>원래 비용 실제 값의 조정 상태가 **조정됨** 으로 업데이트됩니다.</p><p>조정 상태가 **조정 불가능** 인 비용 실제 값 취소가 생성됩니다.</p> | <p>원래 미청구 매출액 실제 값의 조정 상태가 **조정됨** 으로 업데이트됩니다.</p><p>조정 상태가 **조정 불가능** 인 미청구 매출액 실제 값 취소가 생성됩니다.</p> | 해당 없음 | <p>업데이트된 기존 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800, *조정됨*</li><li>**미청구 매출액 실제값:** Bob Kozack, 8시간, USD 1,600 ,*조정됨*</li></ul><p>이전 재무 영향을 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, (8시간), (USD 800), *조정 불가능*</li><li>**미청구 매출액 실제값:** Bob Kozack, (8시간), (USD 1,600), *조정 불가능*</li></ul> |
| 시간 입력은 승인된 후 회수됩니다. | <p>원래 비용 실제 값의 조정 상태가 **조정됨** 으로 업데이트됩니다.</p><p>조정 상태가 **조정 불가능** 인 비용 실제 값 취소가 생성됩니다.</p> | <p>원래 미청구 매출액 실제 값의 조정 상태가 **조정됨** 으로 업데이트됩니다.</p><p>조정 상태가 **조정 불가능** 인 미청구 매출액 실제 값 취소가 생성됩니다.</p> | 해당 없음 | <p>업데이트된 기존 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800, *조정됨*</li><li>**미청구 매출액 실제값:** Bob Kozack, 8시간, USD 1,600 ,*조정됨*</li></ul><p>이전 재무 영향을 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, (8시간), (USD 800), *조정 불가능*</li><li>**미청구 매출액 실제값:** Bob Kozack, (8시간), (USD 1,600), *조정 불가능*</li></ul> |
| 계약이 확정됩니다. | <p>기존 비용 실제 값의 조정 상태가 **조정됨** 으로 업데이트됩니다.</p><p>조정 상태가 **조정 불가능** 인 비용 실제 값 취소가 생성됩니다.</p><p>계약 규칙을 재평가한 후 새로운 비용 실제 값이 생성됩니다.</p> | <p>기존 미청구 매출액 실제 값의 조정 상태가 **조정됨** 으로 업데이트됩니다.</p><p>조정 상태가 **조정 불가능** 인 미청구 매출액 실제 값 취소가 생성됩니다.</p><p>계약 규칙을 재평가한 후 새로운 미청구 매출액 실제 값이 생성됩니다.</p> | 해당 없음 | <p>업데이트된 기존 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800, *조정됨*</li><li>**미청구 매출액 실제값:** Bob Kozack, 8시간, USD 1,600 ,*조정됨*</li></ul><p>이전 재무 영향을 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, (8시간), (USD 800), *조정 불가능*</li><li>**미청구 매출액 실제값:** Bob Kozack, (8시간), (USD 1,600), *조정 불가능*</li></ul><p>재평가된 재무 영향에 대해 생성된 신규 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800</li><li>**미청구 매출액 실제값**: Bob Kozack, 8시간, USD 1,600</li></ul> |
| 송장이 생성됩니다. | 해당 없음 | 해당 없음 | 해당 없음 | |
| 송장이 확인됩니다. 미청구 매출액 실제 값의 수량에서 송장 라인 상세 내역의 수량에는 변경 사항이 없습니다. | 해당 없음 | <p>이전 미청구 매출액 실제 값의 송장 상태가 업데이트됩니다.</p><p>조정 상태가 **조정 불가능** 인 미청구 매출액 실제 값 취소가 생성됩니다. | 청구 매출액 실제 값이 생성됩니다. | <p>변경되지 않은 기존 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800</li></ul><p>업데이트된 기존 실제 값:</p><ul><li>**미청구 매출액 실제 값:** Bob Kozack, 8시간, USD 1,600, *전기된 고객 송장*</li></ul>진행 중인 재무 작업(WIP)을 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**미청구 매출액 실제값**: Bob Kozack, (8시간), (USD 1,600)</li></ul><p>청구 매출액 실제 값을 기록하기 위해 생성된 신규 실제 값:</p><ul><li>**청구 매출액 실제값**: Bob Kozack, 8시간, USD 1,600</li></ul> |
| 송장 라인 상세 내역의 수량이 미청구 매출액 실제 값의 수량에서 감소한 후 송장이 확정됩니다. | 해당 없음 | <p>원래 미청구 매출액 실제 값의 조정 상태가 **조정됨** 으로 업데이트됩니다.</p><p>미청구 매출액 실제 값 취소는 원래 미청구 매출액 실제 값에 대해 생성됩니다. 조정 상태는 **조정 불가능** 입니다.</p><p>두 개의 신규 미청구 매출액 실제 값이 생성됩니다.</p><ul><li>청구 가능 시간에 대해 청구 가능한 미청구 매출액 실제 값</li><li>잔여 수량에 대해 청구 불가능한 미청구 매출액 실제 값</li></ul><p>미청구 매출액 실제 값 취소는 두 개의 신규 미청구 매출액 실제 값에 대해 생성됩니다.</p> | <p>두 개의 청구 매출액 실제 값이 생성됩니다.</p><ul><li>청구 가능 시간에 대해 청구 가능한 청구 매출액 실제 값</li><li>잔여 수량에 대해 청구 불가능한 청구 매출액 실제 값</li></ul> | <p>변경되지 않은 기존 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800</li></ul><p>업데이트된 기존 실제 값:</p><ul><li>**미청구 매출액 실제값:** Bob Kozack, 8시간, USD 1,600 ,*조정됨*</li></ul><p>이전 재무 WIP를 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**미청구 매출액 실제값:** Bob Kozack, (8시간), (USD 1,600), *조정 불가능*</li></ul><p>업데이트된 매출액 WIP를 기록하기 위해 생성된 신규 실제 값:</p><ul><li>**미청구 매출액 실제값:** Bob Kozack, 6시간, USD 1,200 ,*청구 가능*</li><li>**미청구 매출액 실제값:** Bob Kozack, 2시간, USD 400 ,*청구 불가능*</li></ul><p>업데이트된 매출액 WIP를 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**미청구 매출액 실제값:** Bob Kozack, (6시간), (USD 1,200), *청구 가능*</li><li>**미청구 매출액 실제값:** Bob Kozack, (2시간), (USD 400), *청구 불가능*</li></ul><p>청구 매출액 값을 기록하기 위해 생성된 신규 실제 값:</p><ul><li>**청구 매출액 실제값:** Bob Kozack, 6시간, USD 1,200, *청구 가능*</li><li>**청구 매출액 실제값:** Bob Kozack, 2시간, USD 400, *청구 불가능*</li></ul> |
| 송장 라인 상세 내역의 수량이 미청구 매출액 실제 값의 수량에서 증가한 후 송장이 확정됩니다. | 해당 없음 | <p>원래 미청구 매출액 실제 값의 조정 상태가 **조정됨** 으로 업데이트됩니다.</p><p>미청구 매출액 실제 값 취소는 원래 미청구 매출액 실제 값에 대해 생성됩니다. 조정 상태는 **조정 불가능** 입니다.</p><p>새 수량에 대해 신규 미청구 매출액 실제값이 생성됩니다.</p><p>미청구 매출액 실제 값 취소는 신규 미청구 매출액 실제 값에 대해 생성됩니다.</p> | 새 수량에 대해 청구 매출액 실제값이 생성됩니다. | <p>변경되지 않은 기존 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800</li></ul><p>업데이트된 기존 실제 값:</p><ul><li>**미청구 매출액 실제값:** Bob Kozack, 8시간, USD 1,600 ,*조정됨*</li></ul><p>이전 재무 WIP를 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**미청구 매출액 실제값:** Bob Kozack, (8시간), (USD 1,600), *조정 불가능*</li></ul><p>업데이트된 매출액 WIP를 기록하기 위해 생성된 신규 실제 값:</p><ul><li>**미청구 매출액 실제값:** Bob Kozack, 10시간, USD 2,000 ,*청구 가능*</li></ul><p>업데이트된 매출액 WIP를 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**미청구 매출액 실제값**: Bob Kozack, (10시간), (USD 2,000), *청구 가능*, *조정 불가능*</li></ul><p>청구 매출액 실제 값을 기록하기 위해 생성된 신규 실제 값:</p><ul><li>**청구 매출액 실제값:** Bob Kozack, 10시간, USD 2,000, *청구 가능*</li></ul> |
| 청구 가능한 수량 또는 가격을 줄이기 위해 송장이 수정됩니다. | 해당 없음 | <p>두 개의 미청구 매출액 실제 값이 생성됩니다.</p><ul><li>수정 송장의 수량에 대한 청구 가능한 미청구 매출액 실제 값</li><li>잔여 수량에 대해 청구 가능한 미청구 매출액 실제 값</li></ul><p>미청구 매출액 실제 값 취소는 두 개의 신규 미청구 매출액 실제 값에 대해 생성됩니다.</p> | <p>청구 매출액 실제 값 취소가 생성됩니다.</p><p>새 수량에 대해 신규 청구 매출액 실제값이 생성됩니다. | <p>변경되지 않은 기존 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800</li><li>**미청구 매출액 실제 값:** Bob Kozack, 8시간, USD 1,600, *전기된 고객 송장*</li><li>**미청구 매출액 실제값**: Bob Kozack, (8시간), (USD 1,600)</li></ul><p>업데이트된 기존 실제 값:</p><ul><li>**청구 매출액 실제값:** Bob Kozack, (8시간), (USD 1,600), *조정됨*</li></ul><p>이전 청구 매출액 실제 값을 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**청구 매출액 실제값:** Bob Kozack, (8시간), (USD 1,600), *조정 불가능*</li></ul><p>수정된 매출액 WIP를 기록하기 위해 생성된 신규 실제 값:</p><ul><li>**미청구 매출액 실제 값:** Bob Kozack, 6시간, USD 1,200, *청구 가능*, *전기된 고객 송장*</li><li>**미청구 매출액 실제값:** Bob Kozack, 2시간, USD 400, *청구 가능*</li></ul><p>수정된 매출액 WIP를 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**미청구 매출액 실제값**: Bob Kozack, (6시간), (USD 1,200), *청구 가능*, *조정 불가능*</li></ul><p>수정된 청구 매출액 실제 값을 기록하기 위해 생성된 신규 실제 값:</p><ul><li>**청구 매출액 실제값:** Bob Kozack, 6시간, USD 1,200, *청구 가능*</li></ul> |
| 청구 가능한 수량 또는 가격을 늘리기 위해 송장이 수정됩니다. | 해당 없음 | <p>새 수량에 대해 신규 미청구 매출액 실제값이 생성됩니다.</p> <p>미청구 매출액 실제 값 취소는 신규 미청구 매출액 실제 값에 대해 생성됩니다.</p> | <p>청구 매출액 실제 값 취소가 생성됩니다.</p>새 수량에 대해 신규 청구 매출액 실제값이 생성됩니다.</p> | <p>변경되지 않은 기존 실제 값:</p><ul><li>**비용 실제 값:** Bob Kozack, 8시간, USD 800</li><li>**미청구 매출액 실제 값:** Bob Kozack, 8시간, USD 1,600, *전기된 고객 송장*</li><li>**미청구 매출액 실제값**: Bob Kozack, (8시간), (USD 1,600)</li></ul><p>업데이트된 기존 실제 값:</p><ul><li>**청구 매출액 실제값:** Bob Kozack, (8시간), (USD 1,600), *조정됨*</li></ul><p>이전 청구 매출액 실제 값을 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**청구 매출액 실제값:** Bob Kozack, (8시간), (USD 1,600), *조정 불가능*</li></ul><p>수정된 매출액 WIP를 기록하기 위해 생성된 신규 실제 값:</p><ul><li>**미청구 매출액 실제 값:** Bob Kozack, 10시간, USD 2,000, *청구 가능*, *전기된 고객 송장*</li></ul><p>수정된 매출액 WIP를 취소하기 위해 생성된 신규 실제 값:</p><ul><li>**미청구 매출액 실제값:** Bob Kozack, (10시간), (USD 2,000), *청구 가능*</li></ul><p>수정된 청구 매출액 실제 값을 기록하기 위해 생성된 신규 실제 값:</p><ul><li>**청구 매출액 실제값:** Bob Kozack, 10시간, USD 2,000, *청구 가능*</li></ul> |

[!INCLUDE[footer-include](../includes/footer-banner.md)]
